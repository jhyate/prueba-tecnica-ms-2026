debug: false

server:
  port: 2027

spring:
  datasource:
    url: jdbc:mariadb://localhost:3306/DB_EMPLOYEES_MS
    username: root
    password: Yohand23
    hikari:
      connection-timeout: 60000
      idle-timeout: 300000
      max-lifetime: 1200000
      minimum-idle: 1
      maximum-pool-size: 100
      validation-timeout: 30000